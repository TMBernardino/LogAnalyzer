# Analisador de Logs LEAF

Ferramenta CLI que processa ficheiros no formato LEAF (Log Event Augmented Format) e produz um relatório estruturado.

## Como correr

(Preencher após implementação: comando para executar, exemplo com system_audit.leaf.)

## Estrutura do projeto

- `src/` — código fonte (parsing, leitura, relatório, entrada CLI)
- `tests/` — testes
- `system_audit.leaf` — ficheiro de log de exemplo
