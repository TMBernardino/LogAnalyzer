# Pasta de ficheiros de log

Coloca aqui os ficheiros `.leaf` (ou `.log` no formato LEAF) que quiseres analisar.

Exemplo: `data/system_audit.log`

Para analisar: `python -m src.main data/system_audit.log` (ajustar conforme o teu CLI.)
